# URL base de la API del backend.
# Vite solo expone al navegador las variables que empiezan por VITE_, para no
# filtrar al bundle configuracion que deba quedarse en el servidor.
VITE_API_URL=http://localhost:8080/api
