/**
 * Capa de controladores: endpoints REST que expone el backend.
 *
 * <p>Los controladores reciben la petición HTTP, delegan en la capa de servicio y
 * devuelven un DTO de respuesta.
 */
package com.fidelidad.programa.controller;
