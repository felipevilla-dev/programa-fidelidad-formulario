/**
 * Capa de validación: validadores de campos y manejo centralizado de excepciones.
 *
 * <p>Incluirá validadores personalizados (por ejemplo, formato del número de
 * identificación) y un {@code @RestControllerAdvice} que traduce las excepciones
 * a respuestas HTTP con un formato de error uniforme.
 */
package com.fidelidad.programa.validation;
