/**
 * Capa de DTO (Data Transfer Object): objetos que viajan entre el frontend y el backend.
 *
 * <p>Separar los DTO de las entidades evita exponer el modelo de base de datos en la
 * API y permite validar exactamente los campos que el formulario envía.
 */
package com.fidelidad.programa.dto;
